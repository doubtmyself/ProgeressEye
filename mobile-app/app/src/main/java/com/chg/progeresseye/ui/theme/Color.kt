package com.chg.progeresseye.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Colors (from HTML mockup) ──
val Primary = Color(0xFF256AF4)
val PrimaryVariant = Color(0xFF1E56C8)
val OnPrimary = Color.White

// ── Dark Theme Surfaces ──
val BackgroundDark = Color(0xFF101622)
val SurfaceDark = Color(0xFF151B28)
val SurfaceContainerDark = Color(0xFF1C2738)
val SurfaceContainerHighDark = Color(0xFF252F40)

// ── Text / Content ──
val OnSurfaceDark = Color(0xFFF1F5F9)       // slate-100
val OnSurfaceVariantDark = Color(0xFF94A3B8) // slate-400
val OutlineDark = Color(0xFF334155)          // slate-700
val OutlineVariantDark = Color(0x0DFFFFFF)   // white/5

// ── Status Colors ──
val StatusRunning = Primary
val StatusComplete = Color(0xFF4ADE80)       // green-400
val StatusStalled = Color(0xFFF59E0B)        // amber-500
val StatusOffline = Color(0xFF64748B)        // slate-500
val ErrorRed = Color(0xFFEF4444)             // red-500

// ── Progress Bar ──
val ProgressTrack = Color(0xFF334155)        // slate-700
val ProgressGradientStart = Primary
val ProgressGradientEnd = Color(0xFF22D3EE)  // cyan-400

// ── Accent ──
val Indigo400 = Color(0xFF818CF8)
val Indigo500 = Color(0xFF6366F1)
