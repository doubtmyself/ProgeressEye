<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash Screen theme (shown before Compose loads) -->
    <style name="Theme.ProgeressEye.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ProgeressEye</item>
    </style>

    <!-- Base app theme (Compose takes over immediately) -->
    <style name="Theme.ProgeressEye" parent="android:Theme.Material.Light.NoActionBar" />

</resources>
