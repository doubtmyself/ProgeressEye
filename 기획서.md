Project: ProgressEye부제: 화면 전체 공유 없이 '진행률'만 쏙 뽑아 전달하는 초경량 원격 모니터링 알리미1. 프로젝트 배경 및 목적배경: Adobe CC의 알림 서비스 축소 및 중단으로 인한 편집자들의 불편함 증대. 기존 원격 제어 앱(팀뷰어 등)은 무겁고, 화면 전체 노출로 인한 보안 및 데이터 소모가 심각함.목적: 사용자가 지정한 **작은 영역(진행바)**만 이미지로 읽어 숫자로 변환(OCR), 모바일로 실시간 동기화하여 언제 어디서든 작업 완료 시점을 확인하게 함.2. 핵심 타겟 (Target Audience)영상 편집자 및 모션 그래픽 디자이너: 프리미어, 에프터 이펙트, 다빈치 리졸브 렌더링 대기자.3D 아티스트: Blender, Cinema 4D 등 고사양 렌더링 작업자.AI 연구원: 며칠씩 걸리는 딥러닝 모델 학습(Epoch) 상태 확인이 필요한 개발자.데이터 전문가: 대용량 파일 업로드/다운로드 및 DB 쿼리 실행 대기자.3. 핵심 시스템 아키텍처 (Human-in-the-Loop)A. PC Agent (Local Helper)영역 선택 (Area Selection): 사용자가 마우스 드래그로 진행바가 있는 영역을 지정.초경량 캡처: 전체 화면이 아닌 지정한 영역(예: 150x30 픽셀)만 10초~1분 단위로 캡처.OCR 판독: 캡처된 이미지에서 숫자(%) 및 남은 시간(Time Remaining) 텍스트 추출.보안 강화: 이미지 자체는 서버로 보내지 않고, 판독된 **'숫자 텍스트'**만 전송.B. Cloud (Firebase)상태 저장: 현재 진행률(%), 예상 완료 시간, PC 온라인 상태 저장.신호 전달: 100% 도달 시 안드로이드 푸시(FCM) 메시지 생성.C. Mobile App (Android)대시보드: 여러 대의 PC 진행 상황을 한눈에 확인.스마트 알림: 완료 알림뿐만 아니라, '진행 멈춤(Freeze) 감지' 알림 제공.안전 액션: 완료 확인 후 폰에서 [PC 종료] 또는 [절전 모드] 명령 수행.4. 기존 솔루션 대비 차별점 (USP)비교 항목기존 원격 제어 앱ProgressEye (본 기획)보안화면 전체 노출 (위험)진행바 영역 외 노출 0% (안전)데이터 소모높음 (영상 스트리밍)극도로 낮음 (숫자 몇 바이트)알림 방식직접 접속해서 확인 필요완료 시 푸시 알림으로 선제 안내범용성각 툴의 API 필요 (복잡)화면에 보이기만 하면 모든 툴 지원5. 단계별 개발 로드맵1단계: 기술 검증 (MVP)파이썬 기반 특정 화면 영역 캡처 모듈 개발.Tesseract 또는 가벼운 AI 모델을 활용한 숫자 OCR 정확도 테스트.2단계: 연동 및 앱 개발Firebase Realtime Database 연동.안드로이드 푸시 알림 및 백그라운드 위젯 구현.3단계: 상용화 패키징설치가 간편한 PC용 실행 파일(.exe) 제작.다국어 지원 및 'PC 원격 종료' 기능 추가.