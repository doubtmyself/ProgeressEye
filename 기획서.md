# Project: ProgressEye

**부제**: 화면 전체 공유 없이 '진행률'만 쏙 뽑아 전달하는 초경량 원격 모니터링 알리미

---

## 1. 프로젝트 배경 및 목적

**배경**: Adobe CC의 알림 서비스 축소 및 중단으로 인한 편집자들의 불편함 증대. 기존 원격 제어 앱(팀뷰어 등)은 무겁고, 화면 전체 노출로 인한 보안 및 데이터 소모가 심각함.

**목적**: 사용자가 지정한 **작은 영역(진행바)**의 막대 채움 비율을 픽셀 분석으로 읽어 진행률(%)로 변환하고, 모바일로 실시간 동기화하여 언제 어디서든 작업 완료 시점을 확인하게 함.

---

## 2. 핵심 타겟 (Target Audience)

- **영상 편집자 및 모션 그래픽 디자이너**: 프리미어, 에프터 이펙트, 다빈치 리졸브 렌더링 대기자.
- **3D 아티스트**: Blender, Cinema 4D 등 고사양 렌더링 작업자.
- **AI 연구원**: 며칠씩 걸리는 딥러닝 모델 학습(Epoch) 상태 확인이 필요한 개발자.
- **데이터 전문가**: 대용량 파일 업로드/다운로드 및 DB 쿼리 실행 대기자.

---

## 3. 핵심 시스템 아키텍처

### A. PC Agent (Local Helper)

- **Google 로그인**: Google 계정으로 인증. 같은 계정의 모바일 앱과 자동 연결.
- **영역 선택 (Area Selection)**: 사용자가 마우스 드래그로 진행바가 있는 영역을 지정.
- **초경량 캡처**: 전체 화면이 아닌 지정한 영역(예: 300x20 픽셀)만 10초~1분 단위로 캡처.
- **막대 픽셀 분석**: 캡처된 이미지에서 진행바의 **채움 색상 vs 빈 영역 색상**을 픽셀 단위로 분석하여 진행률(%) 산출. OCR 없이 순수 이미지 처리만으로 동작.
- **보안 강화**: 이미지 자체는 서버로 보내지 않고, 산출된 **'진행률 숫자'**만 전송.

### B. Cloud (Firebase)

- **Google 인증**: Google Sign-In 기반 계정 인증. 같은 계정 = 같은 작업 공간.
- **상태 저장**: 현재 진행률(%), 예상 완료 시간, PC 온라인 상태 저장.
- **신호 전달**: 100% 도달 시 안드로이드 푸시(FCM) 메시지 생성.

### C. Mobile App (Android)

- **Google 로그인**: 같은 Google 계정으로 로그인하면 등록된 PC 목록 자동 표시.
- **대시보드**: 여러 대의 PC 진행 상황을 한눈에 확인.
- **스마트 알림**: 완료 알림뿐만 아니라, '진행 멈춤(Freeze) 감지' 알림 제공.
- **안전 액션**: 완료 확인 후 폰에서 [PC 종료] 또는 [절전 모드] 명령 수행.

---

## 4. 기존 솔루션 대비 차별점 (USP)

| 비교 항목 | 기존 원격 제어 앱 | ProgressEye (본 기획) |
|-----------|-------------------|----------------------|
| 보안 | 화면 전체 노출 (위험) | 진행바 영역 외 노출 0% (안전) |
| 데이터 소모 | 높음 (영상 스트리밍) | 극도로 낮음 (숫자 몇 바이트) |
| 알림 방식 | 직접 접속해서 확인 필요 | 완료 시 푸시 알림으로 선제 안내 |
| 범용성 | 각 툴의 API 필요 (복잡) | 화면에 진행바가 보이기만 하면 모든 툴 지원 |
| 인식 방식 | - | 막대 픽셀 분석 (OCR 불필요, 텍스트 없는 진행바도 지원) |
| 기기 연결 | - | Google 로그인만으로 자동 연결 (페어링 코드 불필요) |

---

## 5. 단계별 개발 로드맵

### 1단계: 기술 검증 (MVP)

- Google 로그인 인증 구현 (PC + Mobile).
- 파이썬 기반 특정 화면 영역 캡처 모듈 개발.
- 진행바 막대 픽셀 분석 엔진 개발 및 정확도 테스트.
- Firebase Realtime Database 연동.
- 안드로이드 기본 대시보드 구현.

### 2단계: 연동 및 앱 개발

- 안드로이드 푸시 알림 및 멈춤 감지 알림 구현.
- 원격 PC 제어 (종료/절전) 기능 추가.
- 설정 화면 및 알림 커스터마이징.

### 3단계: 상용화 패키징

- 설치가 간편한 PC용 실행 파일(.exe) 제작.
- 다국어 지원 및 홈 위젯 추가.
- Google Play Store 배포.
